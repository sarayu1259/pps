//c program to evaluate the given expression
#include<stdio.h>
#include<math.h>
int main()
{
	double x;double result;
	printf("Enter any number\n");
	scanf("%lf",&x);
	result = (1 - (x/2) + (x,(pow,(2/4))) - (x,(pow,(3/6))));
	printf("The result of the given expression is %lf\n",result);
}
